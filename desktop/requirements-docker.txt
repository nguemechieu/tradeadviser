# Lightweight requirements for Desktop Docker deployment
# Only includes core and UI dependencies, not all extras

# Core dependencies from pyproject.toml
numpy>=1.24,<2.0
pandas>=2.0,<3.0
pandas-market-calendars>=4.4
scikit-learn>=1.3
aiohttp>=3.9
websockets>=10,<11
SQLAlchemy>=2.0
python-dotenv>=1.0
pydantic>=2.0
joblib>=1.3
ta>=0.11
tzdata>=2024.1
urllib3>=2.0
pydantic-settings>=2.0

# UI dependencies
Pillow>=10.0
qrcode>=7.4
PySide6>=6.5
pyqtgraph>=0.13
qasync>=0.26
matplotlib>=3.7

# Distributed (for Redis)
celery>=5.3
redis>=5.0

# Additional runtime needs
psycopg[binary]>=3.2
PyJWT
keyring
